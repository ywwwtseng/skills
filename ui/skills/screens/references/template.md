# 輸出模板

寫入 `docs/ui/screens/`。一個畫面一份檔案，加一份 `README.md`。

## README.md

````markdown
# 畫面規格

> 平台：<iOS / Android / web>　|　最後更新：<YYYY-MM-DD>
> 來源：`docs/domain/model/`（<日期>）、`docs/api/contract.md`（<日期>）

## 畫面地圖

```
啟動 ─→ 登入 ─→ 訂單列表 ─┬─→ 訂單詳情 ─→ 取消訂單（對話框）
                          └─→ 篩選（底部面板）
推播 ────────────────────→ 訂單詳情
```

| 畫面 | 檔案 | 進入點 | 需要登入 |
|---|---|---|---|
| 訂單列表 | `order-list.md` | 首頁分頁 | 是 |
| 訂單詳情 | `order-detail.md` | 列表、推播、深連結 | 是 |

## 導航規則

- **返回**：<每個畫面返回去哪的通則>
- **深連結 / 推播進入**：<返回堆疊怎麼組；目標不存在時去哪>
- **未登入進入需登入畫面**：<跳登入，登入後回到原目標>
- **流程中斷**：<多步驟流程中途離開的處理；草稿保留與否>

## 全域狀態與行為

- **離線**：<全域橫幅？各畫面自理？>
- **登入過期**：<靜默刷新 / 跳登入並記住目標>
- **強制更新**：<對應契約的最低支援版本，低於時顯示什麼>
- **未知錯誤碼**：<預設訊息 + traceId 顯示方式>

## 未落地的命令

| 命令 | 原因 |
|---|---|
````

## `<畫面>.md`

````markdown
# 訂單詳情

> 來源：`order.md#Order`、`BR-order-031`　|　端點：`GET /orders/{id}`
> 進入點：訂單列表、推播、深連結 `app://orders/{id}`

## 用途

<一句話：使用者來這裡要做什麼>

## 顯示

| 元素 | 來源 | 說明 |
|---|---|---|
| 訂單編號 | `order.md#Order.number` | |
| 狀態 | `order.md#Order.status` | 列舉，未知值顯示原始字串 |
| 金額 | `order.md#Order.total` | |

## 動作

| 動作 | 命令 | 端點 | 權限 | 不可用時 |
|---|---|---|---|---|
| 取消訂單 | `Order.cancel` | `POST /orders/{id}/cancel` | 訂單擁有者 | 可按，按了才報錯（期限由伺服器判定） |

## 版面骨架

<資訊層級與元素順序；不含顏色、字級、間距——那些在 /ui:tonal-ui 或 /ui:calm-ui>

## 狀態

| 狀態 | 顯示什麼 | 使用者能做什麼 | 資料怎麼來 |
|---|---|---|---|
| 初次載入 | 骨架屏 | 返回 | `GET /orders/{id}` |
| 空 | 不適用 — 詳情頁一定有目標資源 | | |
| 已被刪除 / 不存在 | 整頁訊息 | 返回列表 | 404 |
| 錯誤 | 整頁錯誤 + 重試 | 重試、返回 | |
| 權限不足 | 整頁訊息 | 返回 | 403 |
| 離線 | 顯示快取 + 離線標記 | 唯讀瀏覽 | 本機快取 |
| 背景喚醒 | 自動重新整理 | — | |
| 推播進入 | 同一般進入 | 返回回到列表 | |

<行動端的狀態逐條走 `references/states.md`，不適用的寫「不適用 — 理由」>

## 錯誤行為

| 錯誤碼 | 出現在哪 | 訊息 | 使用者能做什麼 |
|---|---|---|---|
| `ORDER_CANCEL_WINDOW_EXPIRED` | 對話框 | 已超過可取消期限 | 關閉並重新整理 |
| `FORBIDDEN` | 整頁 | 沒有權限 | 返回 |

## 上游回饋

| # | 類型 | 問題 | 狀態 |
|---|---|---|---|
````
